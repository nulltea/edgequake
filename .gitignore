# Rust build artifacts
target/
# WHY: Cargo.lock is committed for the application binary (edgequake/) to ensure
# reproducible CI builds. SDK libraries ignore their Cargo.lock (standard practice).
sdks/*/Cargo.lock
sdks/**/.build/
**/*.rs.bk
*.pdb

# Vendored oar-ocr fork (local patches), mirrored from ~/repos/oar-ocr via
# `make sync-oar-ocr` before docker build. The fork is the source of truth;
# the vendor copy is only needed because Docker's build context is edgequake/.
edgequake/vendor/

# IDE and editor files
 .idea/
*.swp
*.swo
*~
.DS_Store

# Screenshots / temporary images at repo root
*.png
*.jpg
*.jpeg

# Environment files
.env
target/
edgequake/target/
edgequake/crates/*/target/
.env.*.local
*.env

# Log files
*.log
lightrag.log*
logs/
/.playwright-mcp
specs/
logf/
zz_test_docs/
zz-explore/

# Python artifacts (for LightRAG reference)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
# Don't ignore Next.js lib directory
!**/src/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
ENV/
edgequake_webui/node_modules/
edgequake_webui/.next/
edgequake_webui/.turbo/
edgequake_webui/playwright-report/
edgequake_webui/build_pid.txt
edgequake_webui/tsc_output.txt
edgequake_webui/tsc_pid.txt
node_modules/
# Node.js (for webui)
node_modules/
.bun/
bun.lockb

# Playwright MCP artifacts
.playwright-mcp/

# Database and storage
*.db
*.sqlite
*.sqlite3
rag_storage/
temp/
inputs/
*.index
postgres_data/
pgdata/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# Documentation build
site/
_build/

# Temporary files
*.tmp
*.temp
*.bak
*.orig

# Kubernetes secrets
k8s-deploy/secrets/

# Configuration with secrets
config.ini
*.pem
*.key

open-webui/

# Exploration and scratch work
zz-explore/

# Marketing website (separate project, not tracked here)
edgequake-website/

# Root-level marketing screenshots (safe to regenerate)
/*.png

# OS files
Thumbs.db
ehthumbs.db
specs/
./metals